One-line: a trail entry; the trail is temporal and append-only, unlike the breadcrumb.

```jsx
<TrailChip title="TLS & Certificates" domain="sec" via="LNK" jump onClick={back} />
```

`jump` draws the ⤳ mark and the acorn accent — that divergence must be legible in a screenshot, not only in the DOM.
