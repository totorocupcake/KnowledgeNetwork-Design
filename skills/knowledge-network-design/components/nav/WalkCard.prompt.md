One-line: the "walks through here" offer in a document pane.

```jsx
<WalkCard title="What happens when you load a webpage" meta="stop 3 of 9 — TCP builds a reliable, ordered stream out of unreliable packets" onClick={start} />
```

Always acorn: a walk is the authored path.
