One-line: the domain marker that precedes a node's name everywhere — tree rows, chips, trail, header focus.

```jsx
<DomainDot domain="net" size={9} />
```

Never use a domain colour for chrome, and never use a chrome colour for a domain.
