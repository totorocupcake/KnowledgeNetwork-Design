One-line: the top bar of any P.Kt product surface; it also renders the wordmark, which is plain type because there is no logo asset.

```jsx
<AppHeader product="Studio" corpusLine="one corpus — 53 topics in 6 domains / 89 typed links, hand-authored" focus={{ title: 'TCP & UDP', domain: 'net' }}>
  <CountBadge value={12} label="visited" />
  <PillButton size="sm">reset session</PillButton>
</AppHeader>
```
