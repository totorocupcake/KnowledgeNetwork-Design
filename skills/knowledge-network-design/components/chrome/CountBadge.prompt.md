One-line: a header counter — "12 visited" — with the figure in tabular mono so it does not jitter as it changes.

```jsx
<CountBadge value={23} label="route" tone="walk" />
```

Use `tone="walk"` only when the count is about the authored path.
